#include "pch.h"
#include "AntColony.h"
/*#include <vector>

using namespace std;
AntColony::AntColony()
{
	
}

vector<vector<int>> AntColony::createPheromonesMatrix(int size)
{
	vector<vector<int>> pheromonesMatrix;
	for (int i = 0; i < size; i++)
	{
		vector<int> newColumn;
		for (int j = 0; j < size; j++)
		{
			newColumn.push_back(0);
		}
		pheromonesMatrix.push_back(newColumn);
	}
	return pheromonesMatrix;
}

void AntColony::runAnts(vector<vector<int>> &costsMatrix, int size, int numberOfAnts, float evaporation, int iterations)
{
	vector <vector<int>> pheromonesMatrix = createPheromonesMatrix(size);
	for (int j = 0; j < iterations; j++)
	{
		for (int i = 0; i < numberOfAnts; i++)
		{

			
		}
	}

}*/